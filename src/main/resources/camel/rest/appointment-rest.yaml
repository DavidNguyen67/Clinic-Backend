- rest:
    path: /v1/doctor/appointment
    post:
      - path: /
        to: direct:doctor-appointment-create
    get:
      - path: /
        to: direct:doctor-appointment-list
      - path: /count
        to: direct:doctor-appointment-count
      - path: /statistics
        to: direct:doctor-appointment-statistics
      - path: /{id}
        to: direct:doctor-appointment-get
    patch:
      - path: /{id}
        to: direct:doctor-appointment-update
      - path: /restore/{id}
        to: direct:doctor-appointment-restore
    delete:
      - path: /{id}
        to: direct:doctor-appointment-delete

- rest:
    path: /v1/public/appointment
    get:
      - path: /
        to: direct:public-appointment-list

- rest:
    path: /v1/staff/appointment
    get:
      - path: /
        to: direct:staff-appointment-list
      - path: /statistics
        to: direct:staff-appointment-statistics
      - path: /{id}
        to: direct:staff-appointment-get
    patch:
      - path: /{id}
        to: direct:staff-appointment-update

- rest:
    path: /v1/patient/appointment
    get:
      - path: /
        to: direct:patient-appointment-list
      - path: /statistics
        to: direct:patient-appointment-statistics
      - path: /next
        to: direct:patient-appointment-next
      - path: /{id}
        to: direct:patient-appointment-get
    patch:
      - path: /{id}
        to: direct:patient-appointment-update
    post:
      - path: /
        to: direct:patient-appointment-create